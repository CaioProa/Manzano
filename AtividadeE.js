let potencia = 1; // Inicializa a potência (3^0)

for (let i = 0; i <= 15; i++) {
    console.log(`3 elevado a ${i} é: ${potencia}`);
    potencia *= 3; // Multiplica a potência atual por 3 para o próximo expoente
}

// Inicializa o contador com 1
let contador = 1;

// Enquanto o contador for menor que 200
while (contador < 200) {
    // Verifica se o contador é divisível por 4
    if (contador % 4 === 0) {
        console.log(contador); // Mostra o número
    }
    contador++;
}
